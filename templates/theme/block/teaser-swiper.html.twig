
<div class="block_hero">
    <div class="hero-container">
        <div class="teaser-swiper-wrapper">
            <div class="grid-gradient-container">
                <img src="{{ asset("/build/theme/images/headerbild copy.png")}}" class="teaser-swiper-media-gradient">
                <img src="{{ media_url(block.media) }}" alt="{{ media_parameter(block.media,'alt')}}" class="teaser-swiper-media">
                <div class="swiper-text-container">
                    <div class="swiper-overline">{{ block.overline }}</div>
                    <div class="swiper-headline">{{ block.headline }}</div>
                    <div class="swiper-copy">{{ block.copy }}</div>
                </div>
                <img src="{{ asset("/build/theme/images/headerbild copy.png")}}" class="swiper-media-gradient">
            </div>
        </div>
    </div>
</div>
